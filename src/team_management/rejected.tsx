import { useEffect } from "react";

const RejectInvite = () => {
  useEffect(() => {}, []);

  return <>You rejected the invite. Please head to dashboard.</>;
};

export default RejectInvite;
//this will be called when the user rejects the invite
