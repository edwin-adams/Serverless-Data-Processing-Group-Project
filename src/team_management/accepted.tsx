import { useEffect } from "react";

const AcceptInvite = () => {
  useEffect(() => {}, []);

  return <>You accepted the invite. Please head to dashboard.</>;
};

export default AcceptInvite;
//this will be shown to the user when they have accepted the invite
